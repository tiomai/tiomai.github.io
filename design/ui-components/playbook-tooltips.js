document.addEventListener('DOMContentLoaded',()=>{const tip=document.createElement('div');tip.className='floating-tip';tip.hidden=true;document.body.append(tip);const show=event=>{const target=event.target.closest('[data-tip]');if(!target)return;const box=target.getBoundingClientRect();tip.textContent=target.dataset.tip;tip.hidden=false;tip.style.left=`${Math.min(innerWidth-12,Math.max(12,box.left+box.width/2))}px`;tip.style.top=`${Math.max(12,box.top-8)}px`};const hide=()=>tip.hidden=true;document.addEventListener('pointerover',show);document.addEventListener('focusin',show);document.addEventListener('pointerout',hide);document.addEventListener('focusout',hide)});
