(function(){
  if(window.EXAI_PRACTICE_EXPIRY_UI)return;window.EXAI_PRACTICE_EXPIRY_UI=true;
  const decorate=()=>document.querySelectorAll('.pack[data-entitlement-state="time_up"]:not(.practice-time-up)').forEach(card=>{const button=card.querySelector('button');if(!button)return;button.textContent="Time's up";button.dataset.actionKind='time_up';button.classList.add('time-up');card.classList.add('practice-time-up')});
  const observer=new MutationObserver(decorate);observer.observe(document.documentElement,{childList:true,subtree:true});decorate();
  document.addEventListener('click',event=>{const button=event.target.closest('.pack[data-entitlement-state="time_up"] button');if(!button)return;event.preventDefault();event.stopImmediatePropagation();const card=button.closest('.pack'),backdrop=document.createElement('div');backdrop.className='practice-expiry-backdrop';backdrop.innerHTML=`<section class="practice-expiry-dialog" role="dialog" aria-modal="true"><p class="eyebrow">CHALLENGE COMPLETE</p><h2>Time’s up for this practice</h2><p>Your saved answers will now be submitted. The completed score will be available in Results, and a new set can be started when you return to Practices.</p><div><button type="button" class="secondary-action">Keep this page open</button><button type="button" class="primary-action">Complete and view result</button></div></section>`;document.body.append(backdrop);backdrop.querySelector('.secondary-action').onclick=()=>backdrop.remove();backdrop.querySelector('.primary-action').onclick=async()=>{const action=backdrop.querySelector('.primary-action');action.disabled=true;action.textContent='Completing…';try{await window.EXAI_ADAPTERS.assessmentPlayer.submit({attemptId:card.dataset.attemptId,reason:'time_limit'});const href='/eval/results/';location.href=window.EXAI_FRESH_LOCAL_URL?.(href)||href}catch(error){action.disabled=false;action.textContent='Try again';const message=backdrop.querySelector('.practice-expiry-dialog>p:nth-of-type(2)');message.textContent=error?.message||'The practice could not be completed. Please try again.'}};backdrop.onclick=click=>{if(click.target===backdrop)backdrop.remove()}},true);
})();
