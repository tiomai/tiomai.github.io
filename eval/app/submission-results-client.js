(function(){
  const config=window.EXAI_SUPABASE_CONFIG||{url:'https://txgjkrxrucpuomsddlss.supabase.co',publishableKey:'sb_publishable_EbppflWGikBla7rynVzdAw_XBE34g-o'},session=()=>{const key=Object.keys(localStorage).find(value=>value.startsWith('sb-')&&value.endsWith('-auth-token'));try{return key?JSON.parse(localStorage.getItem(key)):null}catch{return null}},headers=token=>({apikey:config.publishableKey,Authorization:`Bearer ${token}`}),date=value=>new Intl.DateTimeFormat('en',{day:'numeric',month:'short',year:'numeric'}).format(new Date(value)),percent=row=>row.max_score?Math.round(Number(row.score||0)/Number(row.max_score)*100):0,localRows=userId=>{try{return JSON.parse(localStorage.getItem(`exai_submission_history_${userId||'anonymous'}`)||'[]')}catch{return[]}};
  const reviewMarkup=row=>(row.review_payload||[]).map(item=>`<article style="padding:16px 0;border-top:1px solid #e8ecf3"><b>Question ${item.position} · ${item.isCorrect===true?'Correct':item.isCorrect===false?'Incorrect':'Submitted'}</b><p>${item.prompt||''}</p><p><strong>Your answer:</strong> ${Array.isArray(item.response)?item.response.join(', '):(item.options?.[item.response]??item.response??'No answer')}</p>${item.isCorrect===false?`<p><strong>Correct answer:</strong> ${item.options?.[item.correctAnswer]??item.correctAnswer}</p>`:''}${item.explanation?`<p>${item.explanation}</p>`:''}</article>`).join('');
  async function load(){const auth=session(),token=auth?.access_token,userId=auth?.user?.id;let rows=localRows(userId);if(token){const response=await fetch(`${config.url}/rest/v1/submission_snapshots?select=*&order=submitted_at.desc&limit=100`,{headers:headers(token)});if(response.ok){const remote=await response.json(),remoteTimes=new Set(remote.map(item=>item.submitted_at));rows=[...remote,...rows.filter(item=>!remoteTimes.has(item.submitted_at))]}}if(typeof data!=='undefined'){rows.filter(row=>['assessment','adaptive'].includes(row.mode)).reverse().forEach((row,index)=>data.unshift({id:`submission-${row.id||index}`,title:row.pack_title,subject:row.subject,date:date(row.submitted_at),status:'available',score:percent(row),correct:`${row.score} / ${row.max_score}`,time:`${Math.max(1,Math.round((row.elapsed_seconds||0)/60))} min`,feedback:'Review the submitted answers and explanations below.',skills:[],snapshotResponses:row.review_payload||[]}));if(typeof render==='function')render();if(typeof openResult==='function'){const original=openResult;openResult=function(item){original(item);if(!item.snapshotResponses)return;const gate=document.querySelector('.review-gate');if(gate){gate.innerHTML=`<div><b>Submitted responses</b><p>Your saved answers, marking and explanations.</p></div><button>Review answers</button>`;gate.querySelector('button').onclick=()=>{gate.insertAdjacentHTML('afterend',`<section class="feedback"><h3>Answer review</h3>${reviewMarkup({review_payload:item.snapshotResponses})}</section>`);gate.remove()}}}}const challenge=rows.filter(row=>row.mode==='challenge'),list=document.querySelector('.challenge-list');if(list&&challenge.length){list.innerHTML=challenge.map(row=>`<article><div><small>${row.subject.toUpperCase()} · CHALLENGE</small><h3>${row.pack_title}</h3><p>Completed ${date(row.submitted_at)} · ${row.max_score} questions · ${Math.max(1,Math.round((row.elapsed_seconds||0)/60))} min</p></div><b>${percent(row)}%</b><button data-snapshot="${row.id||''}">Review pack</button></article>`).join('');list.onclick=event=>{const button=event.target.closest('[data-snapshot]');if(!button)return;const row=challenge.find(item=>String(item.id||'')===button.dataset.snapshot)||challenge[0];drawer.innerHTML=`<header class="drawer-head"><div><p class="eyebrow">CHALLENGE PRACTICE RESULT</p><h2>${row.pack_title}</h2><p class="meta">${row.subject} · Submitted ${date(row.submitted_at)}</p></div><button class="close">×</button></header><section class="hero"><div class="overall"><div><b>${percent(row)}%</b><span>OVERALL RESULT</span></div></div></section><section class="feedback"><h3>Answer review</h3>${reviewMarkup(row)}</section>`;backdrop.classList.add('open');drawer.querySelector('.close').onclick=()=>backdrop.classList.remove('open')}}}
  }
  addEventListener('DOMContentLoaded',load);
})();
